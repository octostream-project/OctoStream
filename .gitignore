node_modules/
dist/
dist-electron/
/android/
/capacitor-android/
*.log
.DS_Store
.env
.vite/
.gradle/
build/
*.node
*.lock
*.bin
native/**/build/
capacitor-plugins/*/platform-android/.gradle/
capacitor-plugins/*/platform-android/build/
capacitor-plugins/*/android/.gradle/
capacitor-plugins/*/android/build/

# IDE / OS
.idea/
.vscode/
*.iml
local.properties
*.swp

# APKs de release — se suben como assets de release en Disroot (el repo supera la cuota)
releases/*.apk

# APKs de release — se suben como assets de release (repo con cuota de 1GB)
releases/*.apk

# Firma de release (nunca commitear)
*.jks
keystore.properties
